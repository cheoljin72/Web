{
  const fruits = ['apple', 'banana', 'orange']; 
  const result = fruits.join(); // 배열을 문자열로 바꾸는 api
  console.log(result);
}

{
  const fruits = 'apple,banana,orange';
  const result = fruits.split(',');
  console.log(result);
}